name: Build Android APK

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java 11
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '11'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.6'

      - name: Extract zip files if exists
        run: |
          for f in $(find . -maxdepth 2 -name "*.zip"); do
            echo "Unzipping $f..."
            unzip -o "$f"
          done

      - name: Locate and Build Flutter Project
        run: |
          PUBSPEC_PATH=$(find . -name "pubspec.yaml" | head -n 1)
          if [ -z "$PUBSPEC_PATH" ]; then
            echo "ERROR: pubspec.yaml not found!"
            exit 1
          fi
          PROJECT_DIR=$(dirname "$PUBSPEC_PATH")
          echo "Moving to project directory: $PROJECT_DIR"
          cd "$PROJECT_DIR"

          # تعديل شرط الـ SDK في pubspec.yaml تلقائياً ليتوافق مع Flutter 3.19.6
          sed -i 's/sdk: .*/sdk: ">=3.0.0 <4.0.0"/' pubspec.yaml

          # حل تعارض المكتبة
          flutter pub add share_plus:^9.0.0
          flutter pub get

          # البناء المستقر
          flutter build apk --debug --no-tree-shake-icons

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-debug
          path: "**/build/app/outputs/flutter-apk/app-debug.apk"
